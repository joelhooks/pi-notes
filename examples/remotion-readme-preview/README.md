# pi-notes Remotion README preview

A tiny Remotion composition used to generate the README preview image.

```bash
bun install
bun run still
bun run render
```

The still output is committed at `out/pi-notes-preview.jpeg` so the root README can show the example without requiring Remotion at install time.
